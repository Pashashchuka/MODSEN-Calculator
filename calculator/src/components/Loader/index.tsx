import React, { FC } from 'react';

import { Wrapper, Element } from './LoaderStyles';

export const Loader: FC = () => {
  return (
    <Wrapper>
      Modsen
      <Element></Element>
    </Wrapper>
  );
};
